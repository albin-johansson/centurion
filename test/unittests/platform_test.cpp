//#include "platform.hpp"

#include <catch.hpp>
#include <string>

// using namespace centurion;

// TEST_CASE("platform::id", "[platform]")
//{
//  CHECK_NOTHROW(platform::id());
//
////#ifdef __WIN32
////  CHECK(platform::id() == platform::platform_id::windows);
////#elif __APPLE__
////  CHECK(platform::id() == platform::platform_id::mac_osx);
////#elif __linux__
////  CHECK(platform::id() == platform::platform_id::linux);
////#elif __ANDROID__
////  CHECK(platform::id() == platform::platform_id::android);
////#endif
//}
//
// TEST_CASE("platform::name", "[platform]")
//{
//  CHECK(*platform::name() == std::string{SDL_GetPlatform()});
//}
